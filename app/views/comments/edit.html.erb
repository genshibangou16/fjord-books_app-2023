<h1><%= t('views.common.title_edit', name: Comment.model_name.human.downcase) %></h1>
<%= render "form", commentable: @comment.commentable, comment: @comment %>
<br>
<div class="page-nav">
  <%= link_to t('views.common.back', name: i18n_pluralize(Comment.model_name.human.downcase)), polymorphic_path(@commentable) %>
</div>
